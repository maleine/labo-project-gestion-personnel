<!-- ==================== views/rh/besoins-formation-form.ejs ==================== -->
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Demande de Formation</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        :root {
            --sidebar-width: 250px;
        }
        
        /* Layout avec sidebar fixe */
        .main-content {
            margin-left: var(--sidebar-width);
            padding: 30px;
            min-height: 100vh;
        }
    </style>
</head>
<body>
      <%- include('../partials/sidebar', { currentPage: 'formations' }) %>
    
    <div class="main-content">
        <div class="container-fluid px-4 py-4">
            <h2 class="mb-4">Demande de Formation</h2>

            <form method="POST" action="/rh/besoins-formation/create">
                <div class="card">
                    <div class="card-body">
                        <div class="row g-3">
                            <div class="col-md-6">
                                <label class="form-label">Personnel Concerné <span class="text-danger">*</span></label>
                                <select class="form-select" name="personnel_id" required>
                                    <option value="">Sélectionner...</option>
                                    <% personnel.forEach(p => { %>
                                    <option value="<%= p.id %>">
                                        <%= p.prenom %> <%= p.nom %>
                                    </option>
                                    <% }); %>
                                </select>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Demandeur <span class="text-danger">*</span></label>
                                <select class="form-select" name="demandeur_id" required>
                                    <option value="">Sélectionner...</option>
                                    <% personnel.forEach(p => { %>
                                    <option value="<%= p.id %>">
                                        <%= p.prenom %> <%= p.nom %>
                                    </option>
                                    <% }); %>
                                </select>
                            </div>
                            <div class="col-md-8">
                                <label class="form-label">Type de Formation <span class="text-danger">*</span></label>
                                <input type="text" class="form-control" name="type_formation" required
                                       placeholder="Ex: Formation Automate, Habilitation Microbiologie">
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Priorité</label>
                                <select class="form-select" name="priorite">
                                    <option value="Basse">Basse</option>
                                    <option value="Moyenne" selected>Moyenne</option>
                                    <option value="Haute">Haute</option>
                                </select>
                            </div>
                            <div class="col-md-12">
                                <label class="form-label">Justification <span class="text-danger">*</span></label>
                                <textarea class="form-control" name="justification" rows="4" required></textarea>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="mt-3">
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-send me-2"></i> Envoyer la Demande
                    </button>
                    <a href="/rh/besoins-formation" class="btn btn-secondary">
                        <i class="bi bi-x-circle me-2"></i> Annuler
                    </a>
                </div>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>